{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "PowerShell: 現在のファイルを実行 (5.1)",
            "type": "PowerShell",
            "request": "launch",
            "script": "${file}",
            "powerShellVersion": "Windows PowerShell (x64)"
        }
    ]
}
